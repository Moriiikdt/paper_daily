name: Daily Audio/Speech AI Papers

on:
  schedule:
    - cron: "0 1 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

concurrency:
  group: arxiv-daily-deploy
  cancel-in-progress: true

jobs:
  build:
    # Self-trigger guard: skip if last commit was by github-actions[bot]
    if: >-
      github.actor != 'github-actions[bot]' &&
      github.event_name != 'push' ||
      github.event_name == 'push' && !(
        startsWith(github.event.head_commit.author.email, '41898282+github-actions') &&
        endsWith(github.event.head_commit.author.email, '@users.noreply.github.com')
      )
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      id-token: write
    timeout-minutes: 30

    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GH_PAT }}
        persist-credentials: true

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: pip install arxiv requests jinja2 beautifulsoup4 lxml

    - name: Run pipeline
      run: python scripts/fetch_and_filter.py

    - name: Push changes
      run: |
        git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        # Remove any generated files that may have gotten tracked
        git restore --source=HEAD -- index.html list.html reports.json 2>/dev/null || true
        rm -f index.html list.html reports.json
        git add -A
        if git diff --cached --quiet; then
          echo "No changes to commit"
        else
          # Fetch latest remote and reset hard, then apply our staged changes
          git fetch --depth=1 origin main
          git reset --hard origin/main
          git add -A
          git commit -m "Update papers $(date -u +'%Y-%m-%d')" || true
          git push origin main --force-with-lease || echo "Push skipped (concurrent run)"
        fi

    - name: Setup Pages
      uses: actions/configure-pages@v4
      if: success()

    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: '.'

    - name: Deploy
      id: deployment
      uses: actions/deploy-pages@v4
      if: success()
