# ── Generated data (not tracked in git) ──────────────────────────────────────
# daily_json/ and daily_html/ are generated by the pipeline each run.
# They are NOT tracked — no need to pollute git history with generated HTML.
daily_json/
daily_html/

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
*.log
.env
arxiv-client.json

# macOS / editor
.DS_Store
*.swp
test_artifacts/
